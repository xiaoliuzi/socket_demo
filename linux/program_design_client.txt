1）初始化socket套接字地址结构
2）创建套接字
3）向服务器端发起连接请求
4）如果标准输入有字符输入，则继续下一步;否则，跳至第7步
5）如果输入字符为不是文件结束符，则继续下一步;否则，跳至第9步
6）从标准输入读入字符，并输出到server的连接中去
7）如果与服务器连接的套接字有字符可读，则继续下一步;否则，跳至第4步
8）从连接套接字中读入字符，并输出到标准输出，跳回第4步
9）关闭连接套接字
10）程序结束
